# 1.0.0-rc.2

> [! IMPORTANT]
> 我们完成了重构，耗时 **0** 天!

## 重构

* Controls -> UserCard
  + 改用**代码创建控件**而非**Xaml字符串**转换为控件实例
* Utils -> ConfigManager
  + 去除方法名的 `All`
  + 添加方法 `CreateDefaultConfig`, 用于创建默认配置文件
  + 更名 `ReadConifg()` 为 `LoadConfig()`
  + 为读写配置。两个方法添加保护
