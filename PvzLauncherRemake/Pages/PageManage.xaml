<ui:Page x:Class="PvzLauncherRemake.Pages.PageManage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PvzLauncherRemake.Pages"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:ctrl="clr-namespace:PvzLauncherRemake.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageManage" Loaded="Page_Loaded">

    <Grid>
        <Grid x:Name="grid_Main">
            <TabControl x:Name="tabControl" Style="{StaticResource TabControlPivotStyle}">
                <TabItem Header="游戏管理">
                    <ListBox x:Name="listBox" SelectionMode="Single" Background="#00000000" Margin="0,0,0,10">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ctrl:UserCard/>
                        <ctrl:UserCard Icon="beta"/>
                        <ctrl:UserCard/>
                        <ctrl:UserCard/>
                    </ListBox>
                </TabItem>
                <TabItem Header="修改器管理">
                    <ListBox x:Name="listBox_Trainer" Background="#00000000" Margin="0,0,0,10">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ctrl:UserCard Title="Title" Version="1.0.0.0" SupportVersion="1.0.0.0" isActive="True"/>
                    </ListBox>
                </TabItem>
            </TabControl>
            <Button x:Name="button_Load" Content="导入游戏" VerticalAlignment="Top" Click="button_Load_Click" HorizontalAlignment="Right" Margin="0,10,10,0">
                <Button.BorderBrush>
                    <LinearGradientBrush EndPoint="0,3" MappingMode="Absolute">
                        <LinearGradientBrush.RelativeTransform>
                            <ScaleTransform CenterY="0.5" ScaleY="-1"/>
                        </LinearGradientBrush.RelativeTransform>
                        <GradientStop Color="#29000000"/>
                        <GradientStop Color="#33000000" Offset="1"/>
                    </LinearGradientBrush>
                </Button.BorderBrush>
            </Button>


        </Grid>

        <Grid x:Name="grid_Loading" Visibility="Hidden">
            <ui:ProgressRing IsActive="True" Width="50" Height="50"/>
            <TextBlock x:Name="textBlock_Loading" Text="加载中..." VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,100,0,0"/>
        </Grid>
    </Grid>
</ui:Page>
