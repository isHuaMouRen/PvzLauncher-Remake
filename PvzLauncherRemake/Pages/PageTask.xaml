<ui:Page x:Class="PvzLauncherRemake.Pages.PageTask"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PvzLauncherRemake.Pages"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:controls="clr-namespace:PvzLauncherRemake.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageTask">

    <Grid>
        <Grid>
            <Grid Margin="10,10,10,10" VerticalAlignment="Top" >
                <TextBlock x:Name="textBlock_Average" Text="总进度: %" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,10"/>
                <ProgressBar x:Name="progressBar_Average" Background="#19000000" VerticalAlignment="Bottom"/>
            </Grid>
            <ListBox x:Name="listBox_Task" Background="#00000000" Margin="0,40,0,0" VirtualizingPanel.ScrollUnit="Pixel">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="5"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <controls:UserTaskCard/>
                <controls:UserTaskCard/>
                <controls:UserTaskCard/>
            </ListBox>
        </Grid>

        <TextBlock x:Name="textBlock_None" Text="这里似乎什么都没有，去下载页看看吧！" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="25" Visibility="Hidden"/>
    </Grid>
</ui:Page>
